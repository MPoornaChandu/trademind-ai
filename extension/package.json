{
  "name": "trademind-ai-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-static.mjs",
    "check": "tsc --noEmit",
    "validate": "npm run build && node scripts/validate-extension.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.329",
    "typescript": "^5.9.3"
  }
}
